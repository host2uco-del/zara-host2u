version: 1.1.4

cache: true

endpoints:
  custom:
    - name: "OpenRouter"
      apiKey: "${OPENROUTER_KEY}"
      baseURL: "https://openrouter.ai/api/v1"
      models:
        default: ["google/gemini-2.5-pro"]
        fetch: false
      titleConvo: true
      titleModel: "google/gemini-2.5-pro"
      modelDisplayLabel: "Gemini via OpenRouter"
